{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://mgrin.github.io/mgr-pdf-viewer-react",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "react-scripts": "^3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
